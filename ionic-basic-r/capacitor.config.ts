import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.code.ionicbasic',
  appName: 'ionic-basic-APK',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
