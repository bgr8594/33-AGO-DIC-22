import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.firebasetest.org',
  appName: 'ionic-basic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
